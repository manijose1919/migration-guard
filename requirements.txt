# Runtime dependencies (pinned to lower bounds for reproducibility).
sqlparse>=0.5.0
pydantic>=2.7
fastapi>=0.111
uvicorn[standard]>=0.30
tomli>=2.0; python_version < "3.11"   # stdlib tomllib backport for 3.10
